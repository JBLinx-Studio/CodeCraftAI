
export interface TemplateColors {
  blue: boolean;
  green: boolean;
  red: boolean;
  purple: boolean;
  dark: boolean;
  light: boolean;
}

export interface TemplateFeatures {
  responsive: boolean;
  animation: boolean;
  form: boolean;
  navigation: boolean;
}
